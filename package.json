{
  "name": "procan-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && ./node_modules/.bin/prisma generate",
    "build": "npm run build:frontend && npm run build:backend"
  },
  "dependencies": {
    "prisma": "5.22.0",
    "@prisma/client": "5.22.0"
  }
}
