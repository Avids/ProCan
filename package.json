{
  "name": "procan-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "vercel-build": "cd backend && npm install && npx prisma@5.22.0 generate && cd ../frontend && npm install && npm run build",
    "build": "npm run vercel-build"
  },
  "dependencies": {
    "prisma": "5.22.0",
    "@prisma/client": "5.22.0"
  }
}
